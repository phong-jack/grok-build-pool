{
  "name": "grok-pool",
  "version": "4.0.0",
  "description": "Transparent catch-all proxy + account pool for Grok Build — trace everything, premium-first routing, keeps Thinking alive",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "login": "node src/login.js",
    "accounts": "node src/accounts.js",
    "smoke": "node scripts/smoke.js",
    "check-thinking": "node scripts/check-thinking.js",
    "export-accounts": "node scripts/export-accounts.js",
    "test": "node --test tests/golden.test.js",
    "probe-models": "node scripts/probe-models.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}
