Sanitized wire captures from live Grok Build 1.0.13 traffic through the pool (2026-09-06).

- responses-stream/ — POST /v1/responses SSE round-trip (model grok-4.6, tool-call turn)
- session-signals/ — POST /v1/sessions/<uuid>/signals

All Authorization/cookie/token header values redacted at capture time.
